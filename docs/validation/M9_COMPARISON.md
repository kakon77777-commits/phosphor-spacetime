# M9 Comparison Integrity

Comparison reports aggregate numeric metrics from valid runs only while retaining invalid-run counts. Reports mark mixed workloads or unequal baseline seed sets as incompatible instead of silently presenting them as fair comparisons. This is a protocol-integrity signal, not a performance verdict.
